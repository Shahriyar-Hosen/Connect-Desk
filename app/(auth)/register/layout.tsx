import { IChildren } from "@Interface";

export const metadata = {
  title: "Connect Desk | Register Password",
  description: "Generated by Baitul Hikmah Team",
};

const RegisterLayout = ({ children }: IChildren) => (
  <html lang="en">
    <body>
      <main>{children}</main>
    </body>
  </html>
);

export default RegisterLayout;
