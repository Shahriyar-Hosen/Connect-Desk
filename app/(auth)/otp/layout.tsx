import { IChildren } from "@Interface";

export const metadata = {
  title: "Connect Desk | OTP Password",
  description: "Generated by Baitul Hikmah Team",
};

const OTPLayout = ({ children }: IChildren) => (
  <html lang="en">
    <body>
      <main>{children}</main>
    </body>
  </html>
);

export default OTPLayout;
