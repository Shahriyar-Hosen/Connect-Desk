import { HardwareContainer } from "@Components/Hardware";
import PageHeader from "@Components/PageHeader";

const Hardware = () => {
  return (
    <main>
      <PageHeader pageName="Hardware" mainTitle="Hardware" />
      <HardwareContainer />
    </main>
  );
};

export default Hardware;
